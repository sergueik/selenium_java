	<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
<!--
based on:https://github.com/jesuino/jfx8-app
-->
  <modelVersion>4.0.0</modelVersion>
  <!-- change it according your app configuration -->
  <groupId>example</groupId>
  <artifactId>javafx_example</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <properties>
    <log4j.version>1.2.17</log4j.version>
    <jar.final.name>javafx_example_app</jar.final.name>
<main.class>example.FlowPaneEx</main.class>

  </properties>
  <dependencies>
    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
      <version>${log4j.version}</version>
    </dependency>
    <!-- 
    <dependency>
      <groupId>{groupId}</groupId>
      <artifactId>{artifactIT}</artifactId>
      <version>{version}</version>
    </dependency>
   -->
    <dependency>
      <groupId>org.controlsfx</groupId>
      <artifactId>controlsfx-samples</artifactId>
      <version>8.20.7</version>
    </dependency>
  </dependencies>
  <build>
    <finalName>${jar.final.name}</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>2.3.2</version>
        <configuration>
          <source>1.8</source>
          <target>1.8</target>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>2.4</version>
        <configuration>
          <archive>
            <manifest>
              <addClasspath>true</addClasspath>
              <mainClass>${main.class}</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <phase>install</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/lib</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
