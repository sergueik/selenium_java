<?xml version="1.0"?>
<project name="TestNGTest" default="test" basedir=".">
  <!-- http://www.tutorialspoint.com/testng/testng_plug_with_ant.htm -->
  <taskdef name="testng" classname="org.testng.TestNGAntTask">
    <classpath>
      <!-- <pathelement location="${env.USERPROFILE}\.m2\repository\org\testng\testng\6.8.8\testng-6.8.8.jar"/>-->
	  <pathelement location="C:\Users\sergueik\.m2\repository\org\testng\testng\6.8.8\testng-6.8.8.jar"/>
    </classpath>
  </taskdef>
  <property name="testdir" location="test"/>
  <property name="srcdir" location="src"/>
  <property name="libdir" location="c:/java/selenium"/>
  <property name="full-compile" value="true"/>
  <path id="classpath.base"/>
  <path id="classpath.test">
    <pathelement path="${basedir}/"/>
    <fileset dir="${libdir}">
      <include name="**/*.jar"/>
    </fileset>
    <pathelement location="${testdir}"/>
    <pathelement location="${srcdir}"/>
    <path refid="classpath.base"/>
  </path>
  <target name="clean" depends="dir.check" if="dir.exists">
    <delete verbose="${full-compile}">
      <fileset dir="${testdir}" includes="**/*.class"/>
    </delete>
  </target>
   <target name="create" depends="dir.check" unless="dir.exists">
    <mkdir dir="${testdir}"/>
  </target>
    <target name="dir.check">
    <condition property="dir.exists">
      <available file="${testdir}" type="dir"/>
    </condition>
  </target>
  <target name="compile" depends="clean">
    <javac srcdir="${srcdir}" destdir="${testdir}" verbose="${full-compile}">
      <classpath refid="classpath.test"/>
    </javac>
  </target>
  <target name="test" depends="compile">
    <testng outputdir="${testdir}" classpathref="classpath.test">
      <xmlfileset dir="${srcdir}" includes="testng.xml"/>
    </testng>
  </target>
</project>
